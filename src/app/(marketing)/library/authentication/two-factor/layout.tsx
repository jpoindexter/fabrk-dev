/**
 * Two-Factor Authentication Template - Layout with Metadata
 * Auto-generated SEO metadata for optimal discoverability
 */
import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Two-Factor Authentication Template | Fabrk',
  description: '2FA verification page with code input, backup codes, and recovery options.',
  openGraph: {
    title: 'Two-Factor Authentication Template | Fabrk',
    description: '2FA verification page with code input, backup codes, and recovery options.',
    type: 'website',
  },
  twitter: {
    card: 'summary_large_image',
    title: 'Two-Factor Authentication Template | Fabrk',
    description: '2FA verification page with code input, backup codes, and recovery options.',
  },
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
